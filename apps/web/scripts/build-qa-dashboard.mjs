import { mkdir, readFile, readdir, writeFile } from 'node:fs/promises';
import { resolve } from 'node:path';

const reports = resolve(process.cwd(), 'reports');
const resultsDir = resolve(reports, 'allure-results');
const expected = [
  ['UI Tests', 265], ['API Tests', 125], ['BDD Scenarios', 120],
  ['System + Integration', 100], ['k6 Performance', 150],
];
const byCategory = new Map(expected.map(([name, total]) => [name, { name, total, passed: 0, failed: 0, broken: 0, skipped: 0, durationMs: 0 }]));
let screenshots = 0, videos = 0, beforeHooks = 0, afterHooks = 0;
const walk = steps => { for (const step of steps ?? []) { for (const item of step.attachments ?? []) countAttachment(item); walk(step.steps); } };
function countAttachment(item) {
  if (item.type === 'image/png' && /(?:before|after)-screenshot|^screenshot$/i.test(item.name ?? '')) screenshots++;
  if (item.type === 'video/webm' || /video/i.test(item.name ?? '')) videos++;
  if (/before-hook\.json/i.test(item.name ?? '')) beforeHooks++;
  if (/after-hook\.json/i.test(item.name ?? '')) afterHooks++;
}
for (const file of (await readdir(resultsDir)).filter(name => name.endsWith('-result.json'))) {
  const result = JSON.parse(await readFile(resolve(resultsDir, file), 'utf8'));
  const category = result.labels?.find(label => label.name === 'parentSuite')?.value;
  const row = byCategory.get(category);
  if (row) {
    if (result.status in row) row[result.status]++;
    row.durationMs += Math.max(0, (result.stop ?? result.start) - (result.start ?? result.stop));
  }
  for (const attachment of result.attachments ?? []) countAttachment(attachment);
  walk(result.steps);
}
const k6 = JSON.parse(await readFile(resolve(reports, 'k6-summary.json'), 'utf8').catch(() => '{}'));
const k6Metrics = k6.metrics ?? {};
const p95 = k6Metrics['http_req_duration{expected_response:true}']?.values?.['p(95)'] ?? k6Metrics.http_req_duration?.values?.['p(95)'] ?? null;
const requests = k6Metrics.http_reqs?.values?.count ?? null;
const thresholdFailed = Object.values(k6Metrics).some(metric => Object.values(metric.thresholds ?? {}).some(threshold => threshold.ok === false));
const summary = [...byCategory.values()].map(row => {
  if (row.name === 'k6 Performance') row.durationMs = Number(process.env.K6_DURATION?.replace(/s$/, '') ?? 30) * 1000;
  return { ...row, nonPass: row.failed + row.broken + row.skipped, rate: row.total ? row.passed / row.total : 0 };
});
const overall = summary.reduce((a, c) => ({ total: a.total + c.total, passed: a.passed + c.passed, failed: a.failed + c.failed, broken: a.broken + c.broken, skipped: a.skipped + c.skipped, nonPass: a.nonPass + c.nonPass }), { total: 0, passed: 0, failed: 0, broken: 0, skipped: 0, nonPass: 0 });
const playwright = summary.filter(row => row.name !== 'k6 Performance').reduce((a, c) => ({ total: a.total + c.total, passed: a.passed + c.passed, failed: a.failed + c.failed, broken: a.broken + c.broken, skipped: a.skipped + c.skipped }), { total: 0, passed: 0, failed: 0, broken: 0, skipped: 0 });
const payload = JSON.stringify({ categories: summary, overall, playwright, evidence: { screenshots, videos, beforeHooks, afterHooks }, k6: { requests, p95, thresholdFailed, vus: Number(process.env.K6_VUS ?? 10), duration: process.env.K6_DURATION ?? '30s' } });
const html = `<!doctype html><html lang="en" data-theme="blue"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="ProofLens AI QA automation and test evidence report"><title>ProofLens AI · QA Report</title>
<style>
:root{color-scheme:light;--bg:#f2f6f8;--surface:#fff;--text:#172b3a;--muted:#657887;--line:#dbe5eb;--accent:#1769aa;--good:#23845b;--bad:#c34949;--skip:#b96520;--grid:#dfe8ee;--shadow:0 12px 32px #193c5010}:root[data-theme="dark-grey"]{color-scheme:dark;--bg:#1b1e22;--surface:#262a30;--text:#f1f3f5;--muted:#afb8c1;--line:#414750;--accent:#9aa9ba;--good:#61c897;--bad:#ff7373;--skip:#f0aa60;--grid:#424951;--shadow:0 12px 32px #0004}:root[data-theme="green"]{--bg:#f1f7f2;--surface:#fff;--text:#173326;--muted:#62796d;--line:#d8e7dc;--accent:#16734b;--good:#21844f;--bad:#b42318;--skip:#b56a16;--grid:#dbe9df;--shadow:0 12px 32px #143e2010}:root[data-theme="high-contrast"]{color-scheme:light;--bg:#fff;--surface:#fff;--text:#000;--muted:#242424;--line:#000;--accent:#003cff;--good:#006b27;--bad:#b00020;--skip:#8d2d00;--grid:#7a7a7a;--shadow:none}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:14px/1.5 Inter,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}a{color:var(--accent)}header{position:sticky;top:0;z-index:4;display:flex;align-items:center;justify-content:space-between;gap:20px;padding:14px clamp(18px,5vw,68px);background:var(--surface);border-bottom:1px solid var(--line);box-shadow:var(--shadow)}.brand{display:flex;align-items:center;gap:11px;color:var(--text);text-decoration:none;font-weight:800;font-size:16px}.brand img{width:38px;height:38px}.brand small{display:block;color:var(--muted);font-size:10px;letter-spacing:.12em;text-transform:uppercase}.header-actions,.themes,.links{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.theme-button,.links a{border:1px solid var(--line);border-radius:9px;background:var(--surface);color:var(--text);padding:7px 10px;font:inherit;font-size:11px;text-decoration:none;cursor:pointer}.theme-button[aria-pressed="true"]{outline:2px solid var(--accent)}main{max-width:1280px;margin:auto;padding:30px clamp(18px,5vw,68px) 54px}.top{display:flex;justify-content:space-between;align-items:end;gap:20px}.eyebrow{color:var(--accent);font-size:10px;font-weight:800;letter-spacing:.16em;text-transform:uppercase}h1{font-size:clamp(26px,3vw,36px);margin:6px 0;line-height:1.15;letter-spacing:-.04em}.muted{color:var(--muted)}.notice{margin:18px 0;padding:13px 16px;border:1px solid var(--line);border-left:4px solid var(--skip);border-radius:10px;background:var(--surface)}.stats{display:grid;grid-template-columns:repeat(5,1fr);gap:11px;margin:17px 0}.card,.panel,.cat{background:var(--surface);border:1px solid var(--line);border-radius:14px;box-shadow:var(--shadow)}.card{padding:15px}.card span,.cat span,.cat small{display:block;color:var(--muted);font-size:11px}.card strong{display:block;font-size:25px;margin:3px 0}.categories{display:grid;grid-template-columns:repeat(5,1fr);gap:10px}.cat{padding:13px}.cat h3{font-size:12px;margin:0 0 5px}.cat strong{font-size:22px}.cat small{margin-top:5px}.charts{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:13px;margin-top:15px}.panel{padding:16px;min-width:0}.panel.wide{grid-column:1/-1}.panel h2{font-size:15px;margin:0}.panel p{margin:3px 0;color:var(--muted);font-size:11px}svg{width:100%;height:auto;overflow:visible}.axis{fill:var(--muted);font-size:10px}.gridline{stroke:var(--grid)}.passbar{fill:var(--good)}.failbar{fill:var(--bad)}.skipbar{fill:var(--skip)}.durbar{fill:var(--accent)}.legend{display:flex;gap:14px;flex-wrap:wrap;color:var(--muted);font-size:11px;margin:11px 0}.key:before{content:"";display:inline-block;width:9px;height:9px;border-radius:3px;margin-right:5px;background:var(--good)}.key.fail:before{background:var(--bad)}.key.skip:before{background:var(--skip)}.key.dur:before{background:var(--accent)}.tablewrap{overflow:auto;margin-top:18px}table{width:100%;border-collapse:collapse;background:var(--surface)}th,td{text-align:left;padding:10px;border-bottom:1px solid var(--line)}th{font-size:10px;text-transform:uppercase;color:var(--muted)}td.num{text-align:right;font-variant-numeric:tabular-nums}.foot{font-size:11px;color:var(--muted);margin-top:20px}@media(max-width:850px){.stats{grid-template-columns:repeat(3,1fr)}.categories{grid-template-columns:repeat(3,1fr)}.charts{grid-template-columns:1fr}.panel.wide{grid-column:auto}}@media(max-width:600px){header,.top{align-items:flex-start;flex-direction:column}.stats{grid-template-columns:repeat(2,1fr)}.categories{grid-template-columns:repeat(2,1fr)}}
</style></head><body><header><a class="brand" href="index.html"><img src="prooflens-mark.svg" alt="ProofLens AI logo"><span>ProofLens AI<small>QA automation report</small></span></a><div class="header-actions"><span class="muted">Report theme</span><div class="themes"><button class="theme-button" data-theme-choice="dark-grey">Dark grey</button><button class="theme-button" data-theme-choice="blue">Blue</button><button class="theme-button" data-theme-choice="green">Green</button><button class="theme-button" data-theme-choice="high-contrast">High contrast</button></div></div></header><main><section class="top"><div><div class="eyebrow">Latest executed QA run</div><h1>ProofLens AI test quality & performance</h1><p class="muted">610 Playwright cases + 150 k6 case targets · current local execution</p></div><nav class="links"><a href="http://127.0.0.1:4040" target="_blank" rel="noreferrer">Open Allure details ↗</a><a href="ui-report.html">UI</a><a href="api-report.html">API</a><a href="bdd-report.html">BDD</a><a href="system-integration-report.html">System</a><a href="k6-cases-report.html">k6</a></nav></section><div class="notice" id="run-notice"></div><section class="stats" id="stats"></section><h2>All test categories</h2><section class="categories" id="categories"></section><section class="charts"><article class="panel"><h2>Category pass rate · recorded full runs</h2><p>Previous complete run compared with this current evidence run.</p><div class="legend"><span class="key">Previous full run</span><span class="key fail">Current run</span><span class="key skip">Current skipped</span></div><svg id="pass-chart" viewBox="0 0 740 300" role="img" aria-label="Current run pass rate by category"></svg><p class="foot">Previous results are summarized in the provenance notes; current category values come from this Allure run.</p></article><article class="panel"><h2>Non-pass cases by category</h2><p>Separates test failures from cases that did not execute.</p><div class="legend"><span class="key fail">Failed or broken</span><span class="key skip">Skipped</span></div><svg id="status-chart" viewBox="0 0 740 300" role="img" aria-label="Failed and skipped cases by category"></svg><p class="foot">Any skipped cases are shown separately from cases that ran and failed.</p></article><article class="panel wide"><h2>Execution duration by category</h2><p>Sum of observed Playwright case durations; k6 reflects its configured load window.</p><div class="legend"><span class="key dur">Measured time</span></div><svg id="duration-chart" viewBox="0 0 1000 330" role="img" aria-label="Observed test duration by category"></svg><p class="foot">Parallel Playwright case-duration sums are not wall-clock suite duration. Parallel Playwright case-duration sums are not wall-clock suite duration.</p></article></section><h2>Run details</h2><div class="tablewrap"><table><thead><tr><th>Category</th><th>Cases</th><th>Passed</th><th>Failed</th><th>Broken</th><th>Skipped</th><th>Pass rate</th></tr></thead><tbody id="rows"></tbody></table></div><p class="foot" id="evidence"></p></main><script>
const data=${payload};const categories=data.categories;const prior={'UI Tests':262/265,'API Tests':1,'BDD Scenarios':1,'System + Integration':1,'k6 Performance':1};
const total=data.overall;const p95=data.k6.p95;const fmt=n=>Number(n||0).toLocaleString();const pct=n=>(n*100).toFixed(1)+'%';
const stats=[['Cases in report',total.total,'760 target cases'],['Passed',total.passed,pct(total.passed/total.total)+' of listed cases'],['Failed / broken',total.failed+total.broken,total.failed+' failed · '+total.broken+' broken'],['Skipped',total.skipped,'not executed or not reached'],['k6 p95',p95===null?'—':(p95/1000).toFixed(2)+' s',fmt(data.k6.requests)+' measured requests · '+data.k6.vus+' VUs']];
document.getElementById('stats').innerHTML=stats.map(([a,b,c])=>'<article class="card"><span>'+a+'</span><strong>'+b+'</strong><small class="muted">'+c+'</small></article>').join('');
const k6Cases=categories.find(c=>c.name==='k6 Performance');document.getElementById('run-notice').innerHTML='<strong>Current run status:</strong> '+total.passed+'/'+total.total+' cases passed; '+(total.failed+total.broken)+' failed or errored and '+total.skipped+' skipped. The Playwright run had '+playwright.passed+'/'+playwright.total+' pass, '+(playwright.failed+playwright.broken)+' failure/error, and '+playwright.skipped+' skipped. k6 executed '+k6Cases.passed+'/'+k6Cases.total+' checks, measured '+fmt(data.k6.requests)+' requests, and p95 '+(p95/1000).toFixed(2)+' s; threshold '+(data.k6.thresholdFailed?'failed':'passed')+'. Review failed cases and environment load before treating this run as a release pass.';
document.getElementById('categories').innerHTML=categories.map(c=>'<article class="cat"><h3>'+c.name+'</h3><strong>'+c.passed+'/'+c.total+'</strong><span>passed · '+pct(c.rate)+'</span><small>'+(c.failed+c.broken)+' failed/broken · '+c.skipped+' skipped</small></article>').join('');
document.getElementById('rows').innerHTML=categories.map(c=>'<tr><td>'+c.name+'</td><td class="num">'+c.total+'</td><td class="num">'+c.passed+'</td><td class="num">'+c.failed+'</td><td class="num">'+c.broken+'</td><td class="num">'+c.skipped+'</td><td class="num">'+pct(c.rate)+'</td></tr>').join('');
document.getElementById('evidence').textContent='Allure case attachments counted: '+fmt(data.evidence.screenshots)+' screenshots, '+fmt(data.evidence.videos)+' videos, '+fmt(data.evidence.beforeHooks)+' before-hook JSON and '+fmt(data.evidence.afterHooks)+' after-hook JSON attachments. API-only and k6 cases attach API/performance evidence, not browser screenshots.';
const ns='http://www.w3.org/2000/svg', el=(tag,attrs={},text='')=>{const e=document.createElementNS(ns,tag);for(const [k,v]of Object.entries(attrs))e.setAttribute(k,String(v));if(text)e.textContent=text;return e},text=(svg,x,y,t,anchor='middle')=>svg.append(el('text',{x,y,'text-anchor':anchor,class:'axis'},t));
function bars(){const s=document.getElementById('pass-chart'),l=52,r=720,t=25,b=230;[0,25,50,75,100].forEach(v=>{const y=b-v/100*(b-t);s.append(el('line',{x1:l,y1:y,x2:r,y2:y,class:'gridline'}));text(s,l-8,y+3,v+'%','end')});const w=(r-l)/categories.length;categories.forEach((c,i)=>{const x=l+w*(i+.5),base=prior[c.name]*100,now=c.rate*100;s.append(el('rect',{x:x-24,y:b-base/100*(b-t),width:20,height:base/100*(b-t),rx:3,class:'passbar',opacity:.5}));s.append(el('rect',{x:x+3,y:b-now/100*(b-t),width:20,height:now/100*(b-t),rx:3,class:'passbar'}));text(s,x,b+24,c.name.replace('System + Integration','System').replace(' Performance',''))});}
function statuses(){const s=document.getElementById('status-chart'),l=52,r=720,t=25,b=230,max=Math.max(5,...categories.map(c=>c.nonPass));for(let v=0;v<=max;v+=Math.max(1,Math.ceil(max/5))){const y=b-v/max*(b-t);s.append(el('line',{x1:l,y1:y,x2:r,y2:y,class:'gridline'}));text(s,l-8,y+3,String(v),'end')};const w=(r-l)/categories.length;categories.forEach((c,i)=>{const x=l+w*(i+.5),f=c.failed+c.broken,fh=f/max*(b-t),sh=c.skipped/max*(b-t);s.append(el('rect',{x:x-23,y:b-fh,width:19,height:fh,rx:3,class:'failbar'}));s.append(el('rect',{x:x+2,y:b-sh,width:19,height:sh,rx:3,class:'skipbar'}));text(s,x,b+24,c.name.replace('System + Integration','System').replace(' Performance',''))})}
function durations(){const s=document.getElementById('duration-chart'),l=70,r=970,t=28,b=255,max=Math.max(1,...categories.map(c=>c.durationMs/60000));[0,.25,.5,.75,1].forEach(f=>{const y=b-f*(b-t);s.append(el('line',{x1:l,y1:y,x2:r,y2:y,class:'gridline'}));text(s,l-8,y+3,(max*f).toFixed(1)+'m','end')});const w=(r-l)/categories.length;categories.forEach((c,i)=>{const x=l+w*(i+.5),h=c.durationMs/60000/max*(b-t);s.append(el('rect',{x:x-25,y:b-h,width:50,height:h,rx:4,class:'durbar'}));text(s,x,b+24,c.name.replace('System + Integration','System').replace(' Performance',''));text(s,x,Math.max(t+12,b-h-6),(c.durationMs/60000).toFixed(1)+'m')})}
bars();statuses();durations();const root=document.documentElement,buttons=[...document.querySelectorAll('[data-theme-choice]')],saved=localStorage.getItem('prooflens-report-theme');if(saved&&['dark-grey','blue','green','high-contrast'].includes(saved))root.dataset.theme=saved;const sync=()=>buttons.forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.themeChoice===root.dataset.theme)));sync();buttons.forEach(b=>b.addEventListener('click',()=>{root.dataset.theme=b.dataset.themeChoice;localStorage.setItem('prooflens-report-theme',root.dataset.theme);sync()}));
</script></body></html>`;
await mkdir(reports, { recursive: true });
await writeFile(resolve(reports, 'analytics-dashboard.html'), html);
await writeFile(resolve(reports, 'index.html'), html);
console.log(`Built truthful ProofLens QA dashboard: ${overall.passed}/${overall.total} passed, ${screenshots} screenshots, ${videos} videos.`);
